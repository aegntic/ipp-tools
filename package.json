{
  "name": "ipp-tools",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "sites/*"
  ],
  "scripts": {
    "start": "lerna run start --parallel",
    "dev": "lerna run dev --scope=ipp-main --stream",
    "dev:ipp": "lerna run dev --scope=ipp-main --stream",
    "dev:cascadevibe": "lerna run dev --scope=cascadevibe --stream",
    "build": "lerna run build",
    "build:ipp": "lerna run build --scope=ipp-main",
    "build:cascadevibe": "lerna run build --scope=cascadevibe",
    "build:all": "lerna run build",
    "deploy": "lerna run deploy",
    "test": "lerna run test",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "lint": "eslint .",
    "generate:visualizations": "node scripts/visualization/generate-glass-visualizations.js"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "svgdom": "^0.1.17",
    "@svgdotjs/svg.js": "^3.2.0",
    "prop-types": "^15.8.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-plugin-react": "^7.34.0",
    "husky": "^9.0.11",
    "lerna": "^8.1.2",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  }
}
